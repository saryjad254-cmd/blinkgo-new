/**
 * @deprecated — re-export from @/lib/foundation.
 * Use `import { formatEUR, formatDate, ... } from '@/lib/foundation'`.
 */
export {
  formatEUR,
  formatEURShort,
  formatDateDE,
  formatDateTimeDE,
  formatTimeDE,
  formatGermanPhone,
  formatDate,
  formatTime,
  formatDateTime,
  relativeTime,
  startOfDay,
  startOfWeek,
  startOfMonth,
  isRTL,
  GERMAN_CITIES,
  GERMAN_STREETS,
  type GermanCity,
} from '@/lib/foundation/format';
