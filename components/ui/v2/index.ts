/**
 * v2 UI Library — single source of truth for the new customer experience.
 *
 * Every page should import from this barrel:
 *   import { Button, Input, Card, Badge } from '@/components/ui/v2';
 */

export { Button } from './Button';
export { Input } from './Input';
export { Card } from './Card';
export { Badge } from './Badge';
